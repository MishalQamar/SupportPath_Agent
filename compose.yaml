services:
  app:
    build:
      context: .
    working_dir: /app
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
      - wrangler_config:/root/.config/.wrangler
    stdin_open: true
    tty: true

volumes:
  node_modules:
  wrangler_config:
